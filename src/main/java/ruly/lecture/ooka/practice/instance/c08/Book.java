package ruly.lecture.ooka.practice.instance.c08;

/**
 * 図書館の蔵書1冊を表すクラス。
 * <p>
 * 【仕様】
 * </p>
 * <ul>
 * <li>フィールド：書名(title: String)と、貸出中フラグ(isBorrowed: boolean)を持つ。</li>
 * <li>コンストラクタ：書名を引数に取り、貸出中フラグは false で初期化する。</li>
 * <li>borrowBook(): 自身の状態を確認し、貸出可能ならフラグを true にして true を返す。 既に貸出中なら何もせず false
 * を返す。</li>
 * <li>returnBook(): 自身の貸出中フラグを false に更新する。</li>
 * <li>getTitle(), isBorrowed(): 各フィールドの getter。</li>
 * </ul>
 */
public class Book {
	// TODO: フィールド、コンストラクタ、メソッドを実装してください
}
