package ruly.lecture.ooka.practice.basic;

/**
 * データのクリーニングと統計を行うクラス。
 */
public class DataProcessingService {

	/**
	 * 文字列配列から有効な正の整数を抽出し、合計と最大値を計算する。
	 * <p>
	 * 【仕様】
	 * </p>
	 * <ul>
	 * <li>null配列が渡された場合は null を返す。</li>
	 * <li>数値に変換できない文字列はスキップする。</li>
	 * <li>0以下の数値は集計に含めない。</li>
	 * <li>結果は [合計, 最大値] の形式で返す。有効な数値がない場合は [0, 0] を返す。</li>
	 * </ul>
	 *
	 * @param inputs 文字列の配列
	 * @return [合計値, 最大値] の int配列
	 */
	public int[] processData(String[] inputs) {
		// TODO: ここに実装してください
		return null;
	}
}
