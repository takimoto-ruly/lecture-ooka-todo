1. **命名規則の厳守**：クラス名は `PascalCase`（例：`MyClass`）、メソッド名や変数名は `camelCase`（例：`myMethod`）で記述すること。また、実務で通じる適切な英単語を選択すること。
2. **カプセル化**：クラスのフィールド（変数）は原則 `private` とし、外部からのアクセスが必要な場合はアクセサ（getter/setter）を用意すること。
3. **アノテーション**：親クラスのメソッドを上書きする場合は、必ず `@Override` アノテーションを付与すること。